/*! Fuse.js placeholder (minimal scoring) */
class MiniSearch{
  constructor(docs){this.docs=docs||[]}
  search(q){
    q=q.toLowerCase();
    return this.docs.filter(d=> (d.title+d.url+d.text).toLowerCase().includes(q)).slice(0,50);
  }
}